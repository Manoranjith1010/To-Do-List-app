# PostgreSQL connection string
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/todo_api?schema=public"

# Server
PORT=3000
NODE_ENV=development

# JWT
JWT_SECRET="replace-with-a-long-random-secret-string"
JWT_EXPIRES_IN="1h"
